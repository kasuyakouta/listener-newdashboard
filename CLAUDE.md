\# リスナーダッシュボード - プロジェクト概要



\## 概要

22名のリスナー×208名のプレイヤーの1on1担当割り当てを管理するダッシュボード。

URL: https://skgroup-dx.github.io/listener-newdashboard/ (旧: https://kasuyakouta.github.io/listener-newdashboard/ ／アカウント名変更に伴いドメインのみ変更。リポジトリ自体は同一)



\## 技術スタック

\- フロント: GitHub Pages上の単一HTMLファイル(2,023行)

\- フレームワーク: React 18 + Babel standalone(CDN経由、ビルド不要)

\- バックエンド: Google Apps Script (GAS)

\- GAS URL: index.html 46行目に直接記載(他アプリのようなlocalStorage設定式ではない)



\## このアプリ特有の設計(他アプリと異なる点に注意)

\- \*\*PIN認証はサーバー側で照合\*\*:クライアントでハッシュ化するのではなく、`?pin=`のクエリパラメータでGASに直接送信し、GAS側で検証する方式

\- キャッシュキー`listenerDashboard\_v5\_cache`にスキーマバージョン管理あり(age等の必須フィールドが無い古いキャッシュは自動破棄)

\- ドラッグ\&ドロップでリスナー列の並び替え・プレイヤーの担当変更が可能

\- 業務ルールに基づく自動アラート:産休育休復帰、担当ミスマッチ、中途担当ルール違反、年齢/中途年数による切替予定の事前通知

\- ErrorBoundary実装済み(React側のエラーで画面全体がクラッシュしない設計)

\- Undo機能あり(pushUndo等)



\## 必須ルール(標準スタック)

\- iOS Safari互換性・PWA対応を優先の設計制約とする

\- 日時はローカル時刻で組み立てる(UTCは使わない)

\- フォントは IBM Plex Sans JP / Noto Sans JP



\## 変更時のお願い

\- 複雑な変更は実装前にオプションA/B形式で提案し、承認を得てから実装する

\- 回答は簡潔に、前置きは省略する

\- 業務ルール(年齢切替・中途年数切替・産休復帰通知等)を変更する際は、必ず内容を確認してから進めること

